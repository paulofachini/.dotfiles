name: 🪟 Test Windows Installation

on:
  push:
    branches: [main, "feature/**"]
  pull_request:
    branches: [main]

jobs:
  test-windows:
    name: Windows (Git Bash)
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Status - Implementação pendente (Fase 2)
        shell: bash
        run: |
          echo "⏳ Suporte a Windows (Git Bash + Oh My Zsh) será implementado na Fase 2."
          echo "   Acompanhe: https://github.com/paulofachini/.dotfiles"
