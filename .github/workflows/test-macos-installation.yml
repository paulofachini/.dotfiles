name: 🍎 Test macOS Installation

on:
  push:
    branches: [main, "feature/**"]
  pull_request:
    branches: [main]

jobs:
  test-macos:
    name: macOS
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Status - Implementação pendente (Fase 3)
        run: |
          echo "⏳ Suporte a macOS (Oh My Zsh + Homebrew) será implementado na Fase 3."
          echo "   Acompanhe: https://github.com/paulofachini/.dotfiles"
